spring:
  application:
    name: travel-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/travel_agency
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none

  sql:
    init:
      mode: always